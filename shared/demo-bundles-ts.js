// ===================================
// TYPESCRIPT DEMO BUNDLES
// Mirrors DEMO_CODE in demo-bundles.js but stores TypeScript versions
// of each demo. When the user clicks "Export" with the TypeScript tab
// active, export-demo.js looks the demo up here.
//
// Currently a stub — populate one entry per demo as the matching inline
// TS pane is hand-written in the tutorial HTML. If a demo has no entry
// here, export-demo.js falls back to the JS bundle from DEMO_CODE.
// ===================================

const DEMO_CODE_TS = {
    // TODO: hand-write TS variants of DEMO_CODE entries here.
    // Each key must match a key in shared/demo-bundles.js -> DEMO_CODE.
    //
    // Example:
    //   doublePendulum: `
    //   const canvas = document.getElementById('canvas') as HTMLCanvasElement;
    //   const ctx = canvas.getContext('2d')!;
    //   ...
    //   `,
};
