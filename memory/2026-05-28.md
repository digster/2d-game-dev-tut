# 2026-05-28 — New track: Netcode & Multiplayer (scaffold only)

## Request

"I want to look into multiplayer or netcode. Could it be a track on its
own?" Plan mode → conversation → AskUserQuestion (4 questions) →
ExitPlanMode approved.

User-settled decisions captured in the plan file
([/Users/ishan/.claude/plans/i-want-to-look-ancient-finch.md](../../.claude/plans/i-want-to-look-ancient-finch.md)):

1. **Index integration** — rename the root index "🏰 Game Tracks" section
   to "🏰 Tracks" (no sub-groups); both genre tracks and systems tracks
   live in the same flat group.
2. **Existing seed** — `expert.html#network` ("Network Interpolation",
   line 2132) is **left alone**; the new track diverges from it. Both
   surfaces stand alone.
3. **Transport** — simulated network only. A `FakeNetwork` object with
   sliders for RTT, jitter, packet loss, and reorder probability. Zero
   backend, zero WebRTC, zero BroadcastChannel.
4. **Cross-links to genre tracks** — the netcode track itself will
   include "applied" demos that reuse entities from racing-sim and a
   tiny RTS-style unit. The existing tracks (`racing-sim/`,
   `isometric-strategy/`) are **not edited** in this work.

## Delivered (browser-verified, console-clean)

This commit is the **scaffold only** — landing page + foundational
helpers, all five tier cards locked "Coming soon". The five tiers will
land one at a time on the racing-sim cadence (one tier per commit).

```
netcode/
  index.html              ← landing: 5 tier cards (all locked + "Coming soon"),
                            What You'll Build banner, Prerequisites
                            cross-linking to ../intermediate.html#lerp,
                            ../expert.html#delta-time, ../expert.html#spatial,
                            ../racing-sim/index.html, ../expert.html#network
                            (the existing primer, not the new track's first tier),
                            and a Scaffold Self-Check that proves the helpers
                            load and behave (deterministic seed,
                            end-to-end message delivery, stats counters).
  net/
    seeded-rng.js         ← mulberry32 PRNG. Class on window.SeededRng.
                            API: next(), int(max), range(a,b), chance(p),
                            reseed(seed). Same PRNG racing-sim's procgen
                            uses, so this is consistent with project
                            convention (also: seed=0 is the known
                            degenerate-sequence case for mulberry32, so the
                            constructor nudges to 1 — typed verbatim into
                            the code comment).
    fake-network.js       ← FakeNetwork on window. Sorted-array priority
                            queue, per-message deliverAt = now + rtt/2 +
                            jitter(uniform) + (reorder ? rttMs : 0).
                            Every random decision pulls from the
                            internal SeededRng so the same seed
                            reproduces the same "network weather"
                            twice — substrate for the Expert tier's
                            lockstep/rollback determinism story.
                            API: connect(id) → endpoint with
                            send(targetId, msg) + onMessage(handler);
                            tick(nowMs) drains anything due;
                            setParams({rttMs, jitterMs, lossRate,
                            reorderRate}) for UI sliders; flush();
                            resetStats(). Stats counter
                            {sent, delivered, dropped, reordered}.
```

Root `index.html` edits — four small surgical changes:

1. Section heading renamed: "🏰 Game Tracks — End-to-End Tutorials" →
   "🏰 Tracks — End-to-End Tutorials" (and the same rename in the TOC
   section heading).
2. Intro paragraph broadened from "Full game-build tracks…" to call out
   both genre and systems flavours coexisting.
3. New NETCODE nav-button in the `.nav` grid, after RACE — badge
   `NETCODE` (level-expert), subtitle "Systems track — simulated
   network".
4. New TOC `<li>` after the racing-sim one, with five tier-link
   children all marked "(coming soon)" — same convention racing-sim
   used in its first commit (the four 404s will resolve one by one as
   tiers ship).

## Verification done end-to-end (per the plan's verification section)

Static server: `python3 -m http.server 8765` via the existing
`.claude/launch.json` config (no new server config needed).

1. **`netcode/index.html` loads with no console errors** — verified
   `preview_console_logs(level: 'error')` → "No console logs."
2. **Scaffold self-check passes** — the page's bottom self-check
   element renders:
   - `✅ SeededRng deterministic — seed 42 → [0.6011, 0.4483, 0.8525]`
   - `✅ FakeNetwork delivered "{"hello":"world"}" from client`
   - `✅ Stats: sent=1 delivered=1 dropped=0`
3. **FakeNetwork stress test** — three programmatic checks run from
   the page console:
   - **Loss**: 200 packets at 50% loss → 87 dropped, 113 delivered
     (43.5% drop, within statistical noise for n=200).
   - **Reorder**: 50 packets at 30% reorder → 23 of 50 arrived
     out-of-order in the receiver (proves the simulator actually
     disorders things, not just marks packets).
   - **Determinism**: identical seed (42) → bit-identical stats
     `{delivered: 86, dropped: 14, reordered: 6}` across two runs.
     **This is the property the whole Expert tier rests on.**
4. **Root `index.html` integration** — `🏰 Tracks` heading present,
   `Game Tracks` heading **gone**, NETCODE nav-button renders and
   links to `netcode/index.html`, TOC has the new entry plus five
   tier sublinks.
5. **No regression at `expert.html#network`** — the existing primer
   section still renders, `#network` anchor still scrolls to it,
   `<canvas id="networkDemo">` still present. Console clean.
6. **Screenshot** — landing page renders the title, subtitle, nav row
   (Home / Track Index active / Beginner →), the "What You'll Build"
   green-banner panel.

## Key conventions locked in (inherit for all five future tier commits)

- **Helpers in `netcode/net/`, not `shared/`.** They're netcode-specific
  for now; promoting to `shared/` would imply other tracks should
  depend on them. ARCHITECTURE.md was updated with the general rule:
  helpers used by ≥ 2 tier files in the same track go in a sibling
  subfolder; helpers used across tracks go in `shared/`.
- **`FakeNetwork` endpoint surface mirrors WebSocket/DataChannel**
  (`send(targetId, msg)`, `onMessage(handler)`) so demo code is
  transport-agnostic. If a real backend ever lands later, the demos
  swap in O(1) lines.
- **Reorder is implemented as "occasionally add extra delay"** rather
  than as a queue swap. Same observable effect (later message arrives
  first), simpler code, composes cleanly with jitter — a reordered
  packet is just one with an unusually large jitter sample.
- **`SeededRng` is the only randomness inside `FakeNetwork`.** No
  `Math.random()`. The same (seed, message sequence) tuple ALWAYS
  produces the same stats — proven in stress test #3 above.
- **`mulberry32` reaffirmed as the project's PRNG of choice.**
  Racing-sim's procgen uses it; netcode uses it; future tracks should
  too. Constructor nudges `seed=0` to `1` to dodge the known degenerate
  zero-sequence.
- **The `<details>` collapsible code blocks intentionally absent from
  the landing page** because no tier files ship yet (no code to show).
  The Beginner tier commit will introduce them. `export-demo.js` is
  not loaded yet for the same reason; once tier files exist, each one
  will register its demos in a `bundles-<tier>.js` (or omit
  `data-demo-id` to opt out, same approach racing-sim used in its
  first commit).

## Top-level names exposed by the scaffold (collision-checked)

`window.SeededRng`, `window.FakeNetwork`. Greped `shared/utils.js` and
the rest of the project — neither name appears anywhere else. Future
tier-demos files will inherit this clean-namespace rule (see
[memory/tier-demos-no-toplevel-global-shadowing.md](tier-demos-no-toplevel-global-shadowing.md)).

## Files NOT changed (deliberate)

- `expert.html` — the existing `#network` section is the user-settled
  Fundamentals primer; both surfaces stand alone (decision #2 above).
- `racing-sim/`, `isometric-strategy/`, `voxel-worlds/`, `shaders/` —
  cross-link "applied" demos land **inside** the netcode track's
  Expert/Simulations tiers, not by editing genre tracks (decision #4
  above).
- `LEARNINGS.md` — doesn't exist in this repo. Netcode-specific
  gotchas live in this memory file.
- `.env.example` — no env vars involved (everything client-side,
  zero backend).

## What's next (in order)

Following the racing-sim cadence — one tier per commit:

1. **Beginner tier — Hello, Network.** RTT/jitter/loss/MTU intro, the
   bandwidth tradeoff equation, tick rate vs frame rate, FakeNetwork
   slider UI. Mini-project: two side-by-side "clients" exchanging
   ping/move messages through the FakeNetwork with sliders the user
   drags to see messages arrive late / out of order / lost.
2. **Intermediate tier — Authority & Movement.** Authoritative server
   vs P2P; naive (input → wait → move) → client-side prediction →
   entity interpolation for remote players; snap vs smooth correction.
3. **Advanced tier — Reconciliation, Lag Comp, Compression.** Server
   reconciliation (input ring buffer + replay), lag compensation / hit
   rewind, delta compression, bit-packing & quantization, snapshot
   interpolation vs inputs-only.
4. **Expert tier — Determinism, Lockstep, Rollback, AoI.** Determinism
   primer, lockstep mini-RTS, rollback duelling cars (reuses
   racing-sim car physics — the only cross-track stub), AoI grid,
   anti-cheat primer.
5. **Simulations tier — Everything On.** Master arena with all
   techniques togglable, bandwidth budget calculator, lockstep vs
   rollback side-by-side, AoI heatmap, replay scrubber.

---

# 2026-05-28 (pt.2) — Netcode: Beginner tier ("Hello, Network")

## Request

"Continue with the next iteration." — second iteration on netcode,
following the racing-sim cadence (one tier per commit). Topics fixed by
the landing-page Beginner tier card.

## Delivered (browser-verified, console-clean)

```
netcode/
  beginner.html         ← 6 sections + recap + "next" (intro, anatomy,
                          packet-lane demo, tick-vs-frame demo,
                          bandwidth tradeoff, ping-pong mini-project)
  beginner-demos.js     ← 4 IIFE demos + NET_COLORS + fmt helpers
```

Additive scaffold tweak — `netcode/net/fake-network.js` enqueue now
stamps each packet with `sentAt`, `delay`, and `reordered: bool`
metadata so demos can render progress along a lane. Pure addition; the
existing API (send / onMessage / tick / stats) is unchanged and the
scaffold self-check still passes.

`netcode/index.html` — Beginner tier card flipped to `.active + ready +
"Ready"`; landing-page intro updated to "Beginner tier is ready, next
four land per-commit"; the locked-link nav button replaced with a
regular `Beginner →` button.

Root `index.html` — removed the "(coming soon)" marker from the netcode
Beginner link in the Tracks TOC.

## The four Beginner-tier demos (`net_` namespace reserved for future bundles)

1. **packetLaneDemo** — animated single-direction lane CLIENT → SERVER.
   Auto-sends a packet every 700 ms. Each in-flight packet drawn as a
   labelled circle at progress = (now − sentAt) / delay (which is why
   the FakeNetwork additive tweak was needed). Sliders for RTT
   (0–500 ms), jitter (0–100 ms), loss (0–50%), reorder (0–50%).
   Lost packets shown as a faded red ✕ marker that scrolls and fades
   over 800 ms. Reordered packets tinted purple with a fat halo.
   Pause + reset-stats buttons. Stats line shows
   sent / delivered (% drop) / reordered (%). This is the demo that
   proves the FakeNetwork actually behaves as advertised; everything
   later in the tier rests on the user having seen this one.

2. **tickVsFrameDemo** — TWO horizontal rails: ticks on top, render
   frames on the bottom. Bar moves at constant velocity, bounces at
   edges. Orange SIM ball on the tick rail snaps to each new tick
   position; cyan REN ball on the render rail tracks SIM either by
   snap (latest tick value) or by lerp(prev, curr, tickAccum /
   tickPeriod), toggle-controlled. Slider for tick rate (2–60 Hz).
   The dashed yellow connector between SIM and REN shows the visible
   gap (also reported numerically in the info bar as
   `gap |sim−ren| = N px`). Plants the seed the Intermediate tier's
   entity interpolation will pay off — they're the same technique,
   one on local data and one on data arriving from another machine.

3. **bandwidthCalcDemo** — DOM-driven (no canvas). Four sliders:
   tick rate (1–64 Hz), players (2–100), entities/player (1–50),
   bytes/entity (2–128). Two readout rows: per-client outbound, server
   total upstream. Coloured severity meter (modem / mobile-DSL /
   broadband / "too much") whose fill width is `log10(kbps) /
   log10(10000)` mapped to 0–100% — gives logarithmic feel across the
   four orders of magnitude the equation can produce. Four shipped-game
   presets (Quake-era 10 Hz / 16 players, Valorant-ish 64-tick,
   battle royale 64 players, MMO 2 Hz / many entities). The verdict
   text below the meter calls back to the Expert and Advanced tiers'
   work (AoI for `playerCount`, delta compression + quantization for
   `bytesPerEntity`).

4. **pingPongDemo** (capstone mini-project) — two clients A and B,
   each rendered as a coloured panel inside the canvas, exchanging
   messages through one `FakeNetwork` in the middle. Two-way lane:
   A→B packets drawn on the upper half, B→A on the lower. Buttons
   under each client: "Send ping" (one) and "Send burst (5)" (five
   in quick succession — exposes jitter visibly since 5 sends at
   ~same time get spread out by jitter). Seed input + Reset button
   rebuilds the network with the chosen seed. Per-client stats
   (sent/received counters and a log of the last 5 receives, late
   arrivals tagged `[late]` in purple) plus a global network stats
   line under the canvas. The intentional demonstration: set
   seed=42, click burst A then burst B, write down the global stats;
   reset, repeat → stats land in the exact same place every time.
   That's the substrate the Expert tier's lockstep and rollback
   stories are built on, and the Beginner mini-project proves it
   by hand.

## Key conventions locked in (inherit for all four future tier commits)

- **Colour palette.** `NET_COLORS` is the shared visual family for the
  whole netcode track. Cyan = client, orange = server, green =
  delivered, red = dropped, purple = reordered. Memorised on day one,
  reused in every later tier so the visual language stays consistent
  across all 20+ demos the track will eventually have.
- **Endpoint surface mirrors WebSocket/DataChannel.** Every demo
  uses `endpoint.send(targetId, msg)` and
  `endpoint.onMessage(handler)` — the exact two-method surface a
  real transport would expose. Demo code would survive a swap to a
  real backend transport with minimal change.
- **`MAX_DT = 0.05` clamp** in every per-frame loop. Same value the
  racing-sim track uses. Tab refocus or hot GPU never produces a
  > 50 ms dt that could break animations.
- **FakeNetwork inspection-by-property is intentional.** Demos read
  `net.inFlight` (the sorted in-flight queue) and `net.stats` (the
  counters) directly to render visualisations. Both are public
  properties, not `_`-prefixed — the visualisation use case is a
  first-class user of the helper, not a "reaching into internals"
  smell.
- **Loss/reorder fate detection by stats delta.** When a demo calls
  `endpoint.send(...)` it snapshots `net.stats.dropped` /
  `net.stats.reordered` BEFORE the call and compares AFTER to
  decide what happened to that specific packet. This is more robust
  than wrapping the FakeNetwork internals; it relies only on the
  public stats counters and survives any future change to the
  enqueue strategy.
- **Top-level safe names in `beginner-demos.js`:** `NET_COLORS`,
  `MAX_DT`, `fmtMs`, `fmtKbps`, `fmtBytes`, `formatNumber`. None
  collide with `shared/utils.js` exports — verified with
  `grep -E "^(class|function|const|let|var)" shared/utils.js`
  before write.
- **`<details>` code blocks intentionally OMIT `data-demo-id`** because
  no `netcode/bundles-beginner.js` ships yet. Same opt-out approach the
  racing-sim Beginner tier used to keep the Export buttons silent until
  the bundles file lands as a follow-up.

## Verification done end-to-end (per the plan's verification section)

1. **Scaffold self-check still passes** — proves the additive
   FakeNetwork tweak (`sentAt`, `delay`, `reordered` on each pkt) is
   non-breaking. Three green ticks on the landing page.
2. **`netcode/beginner.html` loads, zero console errors.**
3. **All three canvases (`packetLaneCanvas`, `tickVsFrameCanvas`,
   `pingPongCanvas`) and 32 individual control elements render.**
   Programmatic inventory check returned `missing: []`.
4. **packetLaneDemo running** — pixel sample at lane row confirms
   non-background content; live stats line populated.
5. **tickVsFrameDemo running** — info bar reports live values
   (`tickRate 10 Hz · period 100 ms · fps 120 · gap |sim−ren| = 20.1 px`);
   rails drawn at thin 1px (sampler caught the anti-aliased blend).
6. **bandwidthCalcDemo wiring** —
   - Default state at (20 Hz × 8 × 1 × 24 B): client out 30.7 kbps,
     verdict "Tiny stream — even a 56k modem could keep up."
   - Valorant-ish preset (64 Hz × 10 × 1 × 24 B): client out 122.9
     kbps, verdict updates to "Fine for mobile data and ADSL."
   - Slider drag of bytes → 128: client out grows to 655.4 kbps, label
     updates to "128 B" live.
7. **pingPongDemo end-to-end** —
   - Seed=42, burst A + burst B → 10 sent, 9 delivered, 1 dropped
     (10%); receive logs show out-of-order arrivals on both sides
     (e.g. A got B's pings as #3, #1, #5, #4 — even with reorder at
     2%, jitter alone disorders bursts).
   - **Determinism check** — seed=42 twice with the same actions =
     bit-identical global stats `network sent 10 · delivered 9 ·
     dropped 1 (10%) · reordered 0` across both runs.
8. **Landing page** — Beginner card now `.active + ready + "Ready"`;
   Beginner nav-button is a normal link (the `.locked-link`
   workaround is gone).
9. **Root `index.html`** — netcode TOC Beginner sublink no longer
   marked "(coming soon)"; other four still marked.
10. **No regression at `expert.html#network`** — unchanged file
    (FakeNetwork tweak only affects pages that load FakeNetwork, and
    expert.html doesn't). The existing primer's NetworkEntity class
    is its own self-contained code.

## Edge cases & test ideas surfaced this iteration

- **Burst send + jitter shuffles arrival order even at 0% reorder rate.**
  Five packets sent within ~1 ms can easily land out of order under
  ±20 ms jitter. This is realistic — Internet jitter alone is enough to
  reorder UDP without any "reorder" event class. Worth surfacing as a
  teaching note in the Intermediate tier when entity interpolation has
  to handle out-of-order snapshots.
- **`updateHud` runs on RAF, not immediately after `.click()`.** The
  ping-pong stats counters increment synchronously inside `send()`, but
  the DOM doesn't update until the next animation frame. Programmatic
  tests need to await a frame before reading the DOM. Not a bug;
  documented in the determinism test code.
- **Bandwidth meter saturates at 10 Mbps.** The log-scale cap is fine
  for teaching but a user pushing all sliders to max can reach ~16 Mbps
  and the meter just stays at "TOO MUCH". Could refine the cap or add
  a second "ludicrous" band in a follow-up.

## What's next

- **Intermediate tier — Authority & Movement.** Authoritative server vs
  P2P; naive (input → wait → move) → client-side prediction → entity
  interpolation for remote players; snap vs smooth correction. The
  tick-vs-frame demo from this tier is the template for the Intermediate
  tier's entity-interpolation demo (same toggle, different data source).
