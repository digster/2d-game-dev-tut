# 2026-06-03 — Beginner page: wire up the 5 missing demo Export buttons

## Context / why
User noticed the Matrix Transformations demo (and others) had no "📋 Export" button.
Question started scoped to the beginner page but clarified to mean the whole
**game-math track** (`beginner → intermediate → advanced → expert → simulation →
simulation-v2`). A track-wide survey (`<canvas id>` count vs `data-demo-id` count)
showed every page except **beginner** was already 100% wired. Beginner had 11
interactive demos but only 6 Export buttons. User chose to wire up all 5 gaps.

## What changed
The Export button is injected by `shared/export-demo.js` only onto
`<details data-demo-id="X" data-deps="…">`, and only *works* if `X` is registered in
`DEMO_CODE` + `DEMO_HTML` (`shared/demo-bundles.js`) with each dep in
`DEPENDENCY_BUNDLES` (`shared/dependency-bundles.js`).

Two tiers of work:
- **Already had bundles, just needed wiring:** `vectorBasics`, `vectorPlayground`.
  - `vectorPlayground` → added attrs to the existing "Complete Code: Vector Playground"
    `<details>` (deps `vector2d,clearCanvas,drawGrid,drawVector,clamp,randomFloat`).
  - `vectorBasics` had **no** code-reveal block (the "Understanding Vectors" demo never
    had one — that's why it had no button). Added a NEW `<details class="code-block"
    data-demo-id="vectorBasics" data-deps="vector2d,clearCanvas,drawGrid,drawVector">`
    after the demo's info div.
- **Needed new bundles authored** (adapted from `beginner-demos.js`, rewriting the
  page-specific `canvas`/`info`/var names to the standalone harness `canvas`/`info`,
  keeping control-button ids): `advancedVector`, `advancedTrig`, `matrix`. Added 3
  `DEMO_CODE` + 3 `DEMO_HTML` entries right after the `homingMissile` beginner group.

## Shared dependency changes (`shared/dependency-bundles.js`)
- Added `project()` + `reflect()` to the `vector2d` bundle (copied from `utils.js`).
- Added a new `matrix2d` bundle (from `utils.js` Matrix2D: rotate/scale/translate/
  transformPoint/identity).

## KEY LEARNING — mirror drift
`DEPENDENCY_BUNDLES` is a **hand-copied mirror of `shared/utils.js`** and had silently
drifted: `Vector2D.project`/`.reflect` existed in `utils.js` but NOT in the `vector2d`
export bundle, and `Matrix2D` wasn't a dependency at all. So `advancedVector`/`matrix`
worked on the page (use live `utils.js`) but their *exports* would have thrown.
→ When registering a demo for export, verify every class/method it calls exists in the
**mirror**, not just in `utils.js`. `matrix2d.transformPoint` returns a `Vector2D`, so
list `vector2d` before `matrix2d` in `data-deps`.

## Verification
Served the dir, loaded `beginner.html` in real Chromium (Playwright). Confirmed 11
Export buttons / 11 wired ids, all 5 new ids registered in both `DEMO_CODE` &
`DEMO_HTML`. Generated each of the 5 standalone files and ran them in sandboxed iframes
with an error-capturing shim: **all 5 → zero runtime errors + drew shapes**, including
`advancedVector` (project/reflect) and `matrix` (Matrix2D). Visual screenshot of the
matrix export confirmed the rotating rectangle renders. Cleaned up all test artifacts;
added `.playwright-mcp/`, `exp_*.html`, `verify_*.png` to `.gitignore`.

## Files touched
`beginner.html`, `shared/demo-bundles.js`, `shared/dependency-bundles.js`,
`.gitignore`, `ARCHITECTURE.md`, `README.md`.

---

# Part 2 — follow-up: fix the real issues from the edge-case review

After Part 1, reviewed 5 edge cases. Decided (user) **not to add tests** but to **fix real
issues**. Triage: #2 (TS→JS fallback) and #4 (no-mouse NaN) are **non-bugs** (graceful by
design / already guarded). Fixed the other three:

- **Fix 1 — complete the dependency mirror** (`shared/dependency-bundles.js`): the `vector2d`
  bundle still lagged `utils.js` (missing `static subtract`, `distanceSquared`, `setLength`,
  and the `divide` zero-warn); `matrix2d` lacked `copy()`. Added all, verbatim from `utils.js`.
  These weren't called by any current export (so preventive, not an active break), but now the
  bundle classes are **exact mirrors** — verified by a throwaway Node script comparing
  `getOwnPropertyNames(proto/static)` of live vs bundle classes → 0 missing both ways.
- **Fix 2 — clipboard fallback** (`shared/export-demo.js`): new `copyTextToClipboard(text)` —
  async Clipboard API first, then hidden-`<textarea>` + `execCommand('copy')` (works inside the
  click gesture). So Export works on `file://` / insecure contexts instead of "✗ Error". Handler
  now drives ✓/✗ off the boolean return. Shared → benefits every track's Export button.
- **Fix 3 — FOV logic bug** (`beginner-demos.js` + `shared/demo-bundles.js`, kept in sync): the
  `advancedTrig` `fov` branch set `enemy.angle = atan2(toMouse)` (enemy snapped to face cursor)
  → `angleDiff` always 0 → the cone never gated detection. Replaced with an independent sweep
  `enemy.angle += 0.01`, so the rotating cone actually limits the field of view (hide outside it).
  **Behaviour change to an existing demo** (intentional correction).

## Verification (real Chromium, Playwright MCP)
- FOV: new logic in-cone `true` / out-cone `false`; old logic both `true` (bug confirmed + gone).
- Mirror: `advancedVector` + `matrix` exports regenerate, run with **zero console errors**, render.
- Clipboard (tested against cache-busted served bytes — beware the project's `.js` browser-cache
  quirk): API path ✓, `execCommand` fallback path ✓, both-fail→`false` ✓.

## Files touched (Part 2)
`shared/dependency-bundles.js`, `shared/export-demo.js`, `shared/demo-bundles.js`,
`beginner-demos.js`, `ARCHITECTURE.md`.

## Learning
The hand-mirrored bundle (`DEPENDENCY_BUNDLES` ⟷ `utils.js`) is the recurring hazard: a method
can exist live but not in the mirror, so the *page* works and only the *export* breaks. Keep the
`vector2d`/`matrix2d` bundles exact mirrors; when a demo lives in two places (page + bundle), fix
both together (the FOV bug existed in both copies).

# 2026-06-03 (pt.3) — New track: Bullet Hell / Danmaku — scaffold (Pass 1 of 6)

Prompt: two reference screenshots of a suggested "Bullet Hell / Shoot-'em-Up (Touhou/Cave)" and
"Twin-Stick combat" track — "take inspiration to create a plan, don't copy as-is; make it
comprehensive and complete." Planned in plan-mode (plan file:
`~/.claude/plans/this-is-one-dynamic-snowglobe.md`). Two clarifying answers from the user:
**(1) framing = vertical danmaku (Touhou/Cave)** — keyboard 8-way + focus/slow, tiny hitbox vs
graze-box, spell-card patterns the star (player offense minimal); **(2) Simulations finale = both
tooling + spectacle** (pattern editor + deterministic replay + juice AND a multi-phase boss-rush
capstone). Tier order follows the repo convention (Beginner → Intermediate → **Advanced**
boss-composition → **Expert** performance → Simulations), NOT the reference's perf-before-boss —
keeps it consistent with every sibling track ("make it work, then make it fast").

Delivery is iterative, one tier per pass (mirrors how physics-puzzle shipped). **Pass 1 =
scaffold**, done this session:
- `bullet-hell/engine/` — 3 modules, `bh`/`BH`-prefixed, pre-checked vs `shared/utils.js`,
  reusing `Vector2D` (esp. `Vector2D.fromAngle`):
  - `loop.js` — `bhLoop` (fixed-timestep accumulator, sibling of `pzLoop`/`pfLoop`) +
    `bhInstallKeys` (**canvas-scoped** input: `tabIndex` focusable, swallows arrows/space only
    while focused, clears held keys on blur — chosen over window-global because a danmaku page
    stacks many keyboard demos; mirrors roguelike's `rlInstallCanvasKeys`) + `bhInstallPointer`.
  - `render.js` — `BH` palette (incl. `BH.TAU` — folded into the frozen object so no two engine
    files declare a top-level `const TAU`, which would be an "already declared" error in the
    shared script scope) + parallax starfield (`bhMakeStars`/`bhUpdateStars`/`bhDrawStars`) +
    `bhDrawField` + `bhDrawPlayer` (twin hitbox dot / graze ring, revealed in focus) +
    `bhDrawBullet` (white-core danmaku bullet). Boss/HP-bar helpers deferred to the Advanced pass.
  - `field.js` — `BHField` (spawn/step/cull) + `BHBullet` (inert `turn`/`accel` knobs the
    Intermediate curved-paths lesson switches on). **No collision** in the engine — hit/graze is
    the Beginner lesson (`bhHitTest`/`bhGrazeTest`, inline), same split as physics-puzzle's pure
    `PZWorld`.
- `bullet-hell/index.html` — landing page (roadmap: 5 tier cards, all "Planned"/locked until each
  lands) + a **scaffold self-check** that asserts the globals, proves integration (a bullet falls
  exactly 120px in 1.0s), off-screen culling (count → 0), and **bit-for-bit determinism** (two
  seeded 16-bullet rings step to max diff 0 — the guarantee the Simulations replay rests on), then
  renders the scene with the real `bhDraw*` helpers.
- Wired the index everywhere: root `index.html` (new DANMAKU nav-button + nested TOC with the 5
  tiers marked "(coming)" — no broken links to not-yet-existing tier files), `README.md` (full
  "scaffolded; tiers land iteratively" entry), `ARCHITECTURE.md` (the **5th** per-track engine
  paragraph + the two design calls: canvas-scoped input, inert bullet knobs).

Verified in Chromium via preview tools: all 5 self-check lines green, **console clean (0 errors)**,
screenshot confirms the portrait playfield + starfield + blooming bullet ring + ship with graze
ring. Footguns to carry forward: Vector2D mutates in place (`.copy()` before scaling a shared
vector); no two engine files may declare the same top-level `const` (shared script scope →
"already declared" kills the file silently); bullets are small + fast, so high-speed tunneling is
a deferred Simulations topic (Beginner bullets are slow enough for per-step hit tests).
Next: **Pass 2 — Beginner tier** (`beginner.html` + `beginner-demos.js`, 6 demos, capstone
"First Dodge").

# 2026-06-03 (pt.4) — Bullet Hell: Beginner tier ("One Ship, One Bullet") — Pass 2 of 6

Prompt: "Okay, work on the next iteration." → built the Beginner tier on the Pass-1 scaffold.

`bullet-hell/beginner.html` + `beginner-demos.js` — **6 canvas-guarded IIFEs** (380×420 portrait
playfields), teaching order each adding one idea:
1. `loopDemo` (`bhLoopCanvas`) — the fixed loop + playfield; move a ship, clamped to walls.
2. `moveDemo` (`bhMoveCanvas`) — PRECISE movement: a "normalize diagonals" checkbox demonstrates
   the √2 (41%-too-fast) bug when OFF; focus/slow mode on Shift (sets `player.focused`).
3. `shotDemo` (`bhShotCanvas`) — the bullet as a moving point; angle+speed sliders + Fire/Z spawn
   via `Vector2D.fromAngle(θ,speed)`; `field.step` integrates + culls. Emitter marker + aim arrow.
4. `hitDemo` (`bhHitCanvas`) — hitbox vs graze-box; inline SVG geometry diagram (hull/graze/hitbox)
   + a live slow-rain demo; grazed bullets tint gold; red hit-flash.
5. `streamDemo` (`bhStreamCanvas`) — a cooldown IS a fire-rate; `while(fireTimer<=0){fireTimer+=
   interval}` accumulator (carry remainder so rate is exact) + sine-swept aim.
6. `dodgeDemo` (`bhDodgeCanvas`) — capstone "First Dodge": survive 20s on 3 lives, tightening
   3-shot fan (interval lerps 0.14→0.07s), death-clear + 1.2s mercy i-frames, graze = +100, win/
   lose banner, New-run reset.

**The tier's collision lesson is inline + top-level (console-testable):** `bhDist2` (squared dist,
no sqrt — foreshadows Expert's 10k-bullet perf), `bhHitTest` (`d² ≤ (r₁+r₂)²`), `bhGrazeTest` (in
the graze ring but outside the hit radius). Engine's `BHField` still has NO collision — this is
the "collision is the lesson" split. One-shot `b.grazed` flag so a bullet scores graze once, not
60×/s. Shared inline `bhFocusHint` overlay (canvas-scoped input → "click to focus" call-to-action;
keep hint strings ≤ ~40 chars so they don't clip at 380px width — caught + fixed one that did).

Wired: `bullet-hell/index.html` (Beginner card → Ready + link, added "Beginner →" nav button,
roadmap intro now "Beginner playable, Intermediate next"); root `index.html` TOC Beginner → real
link. Note: changing the card `<div class="tier-card locked">` to `<a ... class="tier-card active">`
also requires flipping its closing `</div>`→`</a>` (matched on the line before the Intermediate card).

Verified in Chromium (preview tools): all 6 canvases present (tabindex=0 ⇒ bhInstallKeys ran),
**console clean (0 errors)** on load and after edits; unit-tested the collision lesson
(on-dot→hit, ring→graze, far→neither — all correct); synthetic input proved focus + movement
(focus()→"focused ✓"; held ArrowRight moved 190→259px ≈ 69px @230px/s over 300ms) and Fire button
→ 3 live bullets; SVG diagram + shortened hints render. Footgun reminders held: moveDir() returns a
fresh normalized vector (safe to .multiply); no top-level const collisions (collision fns are
plain top-level `function`s, unique, non-shadowing). Next: **Pass 3 — Intermediate** (emitter +
polar patterns: rings/fans/spirals/aimed/rose/curved paths; promote `BHEmitter` to engine when
Advanced consumes it).

# 2026-06-03 (pt.5) — Bullet Hell: Intermediate tier ("Patterns Are Polar Equations") — Pass 3 of 6

Prompt: "Okay, work on the next iteration."

`bullet-hell/intermediate.html` + `intermediate-demos.js` — **7 canvas-guarded IIFEs** (420×440),
the math-beautiful tier. Core lesson is the **`BHEmitter`** class (taught inline; PROMOTE to
`engine/emitter.js` in Pass 4 when Advanced consumes it — a planned move): separates the pattern
(`fire(em,field)` callback) from the cadence (`interval`) + a base `angle` it can `spin` (rad/s).
Same `while(timer<=0){timer+=iv}` carried-remainder accumulator as the loop. Two pattern helpers
`bhFireRing` (i/n spacing — no doubled bullet at 0 & 2π) and `bhFireFan` (i/(n-1) — bullets at both
arc ends) round out the promotable toolkit.

Demos: (1) emitter (interval+count sliders, pause); (2) rings & fans (arc slider 20→360 morphs fan
↔ full ring); (3) spiral (arms + spin sliders — "a spiral is a ring whose base angle rotates",
bullets fly straight, emission angle turns); (4) aimed (atan2(player−emitter) centers a fan, player
movable, graze counter); (5) rose (speed = 50+170·|cos(kθ)| per bullet in a ring → petals; odd k→k
petals, even→2k; optional 2nd counter-rotated layer for interference); (6) curved paths (switches
on the engine's INERT `turn`/`accel` BHBullet knobs from the Pass-1 scaffold — payoff of reserving
them; player dodges whips); (7) capstone "Pattern Stage" — a `PHASES[]` timeline (Ring Burst →
Aimed Fan → Twin Spiral, screen-clear between) cycling on a phase timer, graze +100/hit −300. The
timeline is deliberately the seed of Advanced's data-driven spell cards.

Self-contained re-declaration of the small helpers `bhDist2`/`bhHitTest`/`bhGrazeTest`/`bhFocusHint`
(NOT promoted — 12 lines, stable, one tier-demos file per page so no collision; the repo's explicit
"self-contained tier" convention). Decided AGAINST an `engine/collision.js` promotion to avoid
churning shipped Beginner files for tiny stable helpers; revisit if they grow.

Wired: `bullet-hell/index.html` (Intermediate card → Ready+link, nav button, roadmap "Beginner &
Intermediate playable; Advanced next"); root `index.html` TOC Intermediate → real link.

Verified in Chromium: all 7 canvases present, **console clean (0 errors)**; after ~1.1s every
emitter is producing bullets (HUDs: emitter 25 volleys/52 live, spiral 108 live, rose "3 petals"/
250 live, aimed tracking w/ grazes, stage cycling phases w/ score). Screenshots confirm the 3-arm
spiral and the 3-petal rose render correctly. Footguns held: Vector2D.fromAngle returns fresh
vectors (safe); BHEmitter.step guards iv=max(0.001,interval) against a 0-interval while-loop lock.
Next: **Pass 4 — Advanced** (boss HP + spell-card timeline as DATA + phases + sub-emitters + bomb;
move `BHEmitter`+`bhFireRing`/`bhFireFan` to `engine/emitter.js`).

# 2026-06-03 (pt.6) — Bullet Hell: Advanced tier ("The Boss Fight") — Pass 4 of 6

Prompt: "Okay, Work on the next iteration."

**Engine promotion (a MOVE, not a copy):** `BHEmitter` + `bhFireRing` + `bhFireFan` → NEW
`engine/emitter.js` (Advanced is their genuine 2nd consumer). Removed the inline declarations from
`intermediate-demos.js` (replaced with a pointer comment) and added `<script src="engine/emitter.js">`
to BOTH intermediate.html and advanced.html — two top-level `class BHEmitter` on one page would be an
"already declared" crash. Intermediate's collapsible code still TEACHES the source. `engine/render.js`
gained `bhDrawBoss` (rotating hex + pulsing core, reads boss.t) + `bhDrawHpBar` (top-of-field bar,
save/restore-wrapped, uses utils' clamp) — first-used here, reused by Expert/Simulations.

`bullet-hell/advanced.html` + `advanced-demos.js` — **6 canvas-guarded IIFEs** (420×440). Two inline
lesson classes: `BHBoss` (pos sweeps a Lissajous figure — sin(0.8t) on x, sin(1.6t) on y — + HP +
hpFrac) and `BHSpellCard` (an attack as DATA: `steps:[{at,set:{interval/spin/angle/fire},burst,label}]`
+ a clock that applies each due step once; `step()` returns true at duration). BHSpellCard earmarked
for `engine/script.js` when the Simulations boss-rush reuses it. Inline player-offense helpers
`bhTickPlayerShots` (twin upward stream on a 0.075s cd) + `bhResolveShotsVsBoss` (circle test → dmg +
consume shot), and `bhMovePlayer` (shared movement). Demos: (1) bossDemo — entity HP/move/bar + shoot
it down; (2) scriptDemo — one looping spell card (ring→spiral→aimed fan); (3) phaseDemo — HP<50% swaps
card + screen-clear + white flash; (4) subDemo — carrier bullets (tag+age fuse) airburst into rings +
an orbiting turret (2nd emitter; `bhFireRing` only needs `{pos,angle}` so an ad-hoc object works);
(5) bombDemo — X clears the field + i-frames + stock + shockwave fx; (6) capstone duelDemo — 2-phase
boss, full kit (move/focus/Z shoot/X bomb), 3 lives/3 bombs, graze +100, capture bonus +10000 when a
phase is cleared without being hit OR bombing (faulted flag), win/lose.

Wired: bullet-hell/index.html (Advanced card → Ready+link, nav button, roadmap "…Advanced playable;
Expert next"); root index.html TOC Advanced → real link.

Verified in Chromium: all 6 advanced canvases present; **console clean (0 errors)**; HUDs confirm
every demo runs (script card looping, sub 400+ airbursts, bomb stocks, duel scoring/lives/capture).
Unit-tested the offense helpers directly in console (bhTickPlayerShots spawns 2; bhResolveShotsVsBoss
100→90 + consumes the shot) — isolating a false alarm where centered static shots missed the SWEEPING
boss (geometry, not a bug). Intermediate page re-verified after the move: no redeclaration error,
console clean, all 7 demos still emit. Boss-fight screenshot confirms boss + HP bar + fan + cyan
player shots render.

⚠️ WORKFLOW GOTCHA (carry forward): when an engine file is edited mid-session, the preview browser
serves the STALE cached copy — `render.js` loaded WITHOUT bhDrawBoss/bhDrawHpBar, freezing exactly the
3 boss demos (the others were fine). Fix: `fetch(engineUrl, {cache:'reload'})` for each engine file,
THEN location.reload(), before verifying. A plain reload did NOT bust it. (`python -m http.server`
sets no Cache-Control, so the browser heuristically caches.) Always cache-bust engine files before
verifying a pass that edited them.

Next: **Pass 5 — Expert** ("Ten Thousand Bullets": pooling, Struct-of-Arrays in Float32Arrays,
spatial-hash hit detection, culling, render batching; upgrade engine/field.js internals in place).

# 2026-06-03 (pt.7) — Bullet Hell: Expert tier ("Ten Thousand Bullets") — Pass 5 of 6

Prompt: "Okay, work on the next iteration."

**KEY PLAN CORRECTION:** the plan said "upgrade engine/field.js in place to SoA." That would BREAK
the object API (`field.bullets` of objects with `.pos`/`.grazed`) that Beginner→Advanced iterate and
teach against. So I did NOT touch the engine field — it stays the object-based teaching store. The
Expert tier teaches the PRODUCTION stores INLINE as its lesson (honest "promote-on-a-guess was wrong"
correction; updated ARCHITECTURE.md to match). These may promote to engine only if the Simulations
capstone reuses them.

`bullet-hell/expert.html` + `expert-demos.js` — **6 canvas-guarded IIFEs** (440×440), perf-focused
(mostly firehoses, not dodging). Inline lessons: `BHBulletPool` (free-list, PLAIN-NUMBER x/y/vx/vy
fields integrated in place → zero alloc; swap-remove → free list), `BHSoaBullets` (flat Float32Arrays
x/y/vx/vy/r + count; step does in-place integrate + **O(1) swap-remove** cull, never splice),
`BHSpatialHash` (Map keyed "gx,gy", insert + queryNear 3×3), `bhDrawSoa` (batched: 2 fills for the
whole store — one rim pass, one white-core pass; `moveTo` before each `arc` so circles don't connect),
and a `bhMakeMeter`/`bhDrawMeter` rolling work-ms graph with a 16.6ms budget line. Demos: (1) naiveDemo
— objects + `vel.copy()` temp-alloc + splice (the villain); (2) poolDemo — pool → "new allocs last
second: 0"; (3) soaDemo — 10k in Float32Arrays + an inline SVG AoS-vs-SoA memory-layout diagram;
(4) hashDemo — toggle hash, "checks this frame" N→~k, 3×3 grid viz, HONEST key-point that rebuild is
O(N) so the win needs many query points; (5) batchDemo — toggle batched vs per-bullet draw, draw-ms;
(6) capstone stressDemo — 5-arm spiral into a 12000-cap SoA store + spatial-hash hit/graze + cull +
batch, fps/work-ms HUD. expert.html does NOT load field.js/emitter.js (builds its own stores — the
point).

Wired: bullet-hell/index.html (Expert card → Ready+link, nav button, roadmap "…Expert playable;
Simulations finale next"); root index.html TOC Expert → real link. ARCHITECTURE.md promotions
paragraph rewritten (emitter MOVED, render boss helpers added, SoA correctly kept inline, status now
"Beginner→Expert shipped, 25 demos").

Verified in Chromium (cache-busted engine + expert-demos first — the Pass-4 gotcha): all 6 canvases
present; **console clean (0 errors)**. HUD metrics confirm the lessons: pool "0 new allocs/sec";
SoA 6k @ step ~0ms; hash 70 checks vs ~1866 naive; batch 3k @ 0.5ms draw. Bumped stress slider max
16→24 (default 12) so it crosses 10k; at rate 24 it holds **11,044 bullets @ 60 fps, 2.8ms total
work** (step 0.6 + draw 2.2), hashed hit-checks ~330 (vs 11k naive). Screenshot confirms the dense
5-arm spiral + low meter bars under the green budget line.

Next: **Pass 6 — Simulations (FINALE): "Danmaku"** — seeded `BHRng` determinism + input replay,
visual pattern editor emitting spell-card JSON, hitstop/screen-shake/particle juice + bullet-cancel
scoring, and the grand multi-phase boss-rush capstone composing every system. Promote `BHSpellCard`
→ engine/script.js (+ maybe rng/spatial) as Simulations becomes their 2nd consumer.
