# 2026-05-21 — New track: Voxel Worlds (scaffold + Terraria Beginner tier)

## Request
"Learn everything about 2D voxel worlds and manipulation (Terraria-like)"; add
as a new track in the Game Tracks section. Plan mode → approved.

Clarified with the user:
- Track covers **two flavors as separate sub-tracks**: Terraria (tile-grid,
  primary) + Noita (cellular-pixel). Noita must NOT repeat concepts already
  taught in Terraria.
- Iteration 1 scope: scaffold both sub-tracks + fully implement the **Terraria
  Beginner tier only**. Remaining tiers are placeholder cards.
- Directory: `voxel-worlds/`. Noita sub-track = **3 tiers** (scoped to new
  ground), not 5 — site-wide tier-count uniformity is not a constraint.

## Delivered (all browser-verified, console-clean)

**New structure — first track with nested sub-tracks:**
```
voxel-worlds/
  index.html              ← top-level: links both sub-tracks (1 level deep, ../shared/)
  terraria/
    index.html            ← 5-tier roadmap (Beginner Ready, rest In Progress)
    beginner.html         ← full tier: 7 sections + recap
    beginner-demos.js     ← 7 IIFE demos + module-level helpers
    bundles-beginner.js   ← export payload (vox_drawGrid only — see below)
  noita/
    index.html            ← preview: 3 locked tiers + "what's covered / NOT
                            re-covered" prose
```
Sub-track pages are **two levels deep** → `../../shared/…`, `../../<page>.html#…`.

**Terraria Beginner tier — 7 demos (`vox_` prefix, plain Canvas2D):**
1. `voxIntro` — static sprite-world vs voxel-grid comparison diagram.
2. `voxTileCoords` — hover-to-tile pick + TILE-size slider (tile↔screen math).
3. `voxFlatArray` — 2D grid ↔ `Uint8Array` strip synced by a scrubber slider
   (teaches `index = y*W + x`).
4. `voxDrawGrid` — 30×15 hand-authored cross-section, fill/grid-lines/IDs
   toggles. **The one exportable demo.**
5. `voxPalette` — same world, jitter + procedural-speckle layers + seed slider
   (sprite-less texturing via `hash2D`).
6. `voxCamera` — 80×40 world, WASD panning, viewport-culling indicator +
   tiles-drawn counter.
7. `voxLayers` — same 80×40 world + background wall grid, fg/bg/both radio.

Root `index.html`: new VOXEL nav button + **two-level nested TOC** (track →
sub-track → tier) — a new TOC depth for this site.

## Key decisions / learnings
- **Export scaffold only renders `<button>` controls.** `shared/export-demo.js`
  maps `DEMO_HTML.controls` entries to `<button>` only — sliders/checkboxes/
  radios are not rendered. So only `voxDrawGrid` (button-only) is registered in
  `bundles-beginner.js`; the other 6 demos **omit `data-demo-id`** entirely so
  no broken Export button appears. Honest opt-out beats a broken export.
- **Top-level `const`/`class` don't attach to `window`.** Verification probe
  showed `VOX_COLORS`/`VOX_MATERIALS`/`TileWorld` as `undefined` on `window`
  while `function`s resolved — expected JS scoping, not a bug (same as iso's
  `ISO_COLORS`). Verify demos by **pixel-sampling the canvas**, not by reading
  globals.
- **Path depth is the #1 copy-paste risk** when adapting iso templates: every
  two-deep page needs `../../shared/…`. Verified clean (no 404s).
- The 404 on a missing `bundles-<tier>.js` is harmless (Export buttons just
  don't render) — but we ship the file so the console stays quiet.

## Verification
- All 4 new pages: console-clean, navigable from root.
- 7/7 demo canvases render real pixels (sampled via `getImageData`).
- Interactions confirmed: tile-size slider, flat-array scrubber (math
  `4×16+0=64` ✓), draw-grid toggles, palette checkboxes, layers radio, camera
  WASD (drove x 0→48, "583 tiles drawn" stays constant = culling works).
- Export: `generateStandaloneHTML('vox_drawGrid', …)` → valid 8.6 KB HTML doc.
- Cross-links: `../../beginner.html#vectors` resolves to the Vectors section.
- README.md + ARCHITECTURE.md updated (nested sub-track pattern, export-scaffold
  button-only limitation documented).

## Follow-ups (next iterations)
- Terraria Intermediate tier: mouse-to-tile picking, click-to-mine/place,
  falling sand/gravel, AABB player-vs-grid collision.
- Terraria Expert/Advanced/Simulations tiers (worldgen, liquids, lighting,
  persistence, AI).
- Noita sub-track tier content (currently preview-only).
- Plan file: `~/.claude/plans/i-want-to-frolicking-corbato.md`.

---

# 2026-05-21 (pt.2) — Voxel Worlds: Terraria Intermediate tier

## Request
"Work on the next iteration" → the Terraria Intermediate tier.

## Delivered (browser-verified, console-clean)
Three new files in `voxel-worlds/terraria/`:
- `intermediate.html` — 5 sections + recap: picking & tool reach, mine/place &
  hotbar, falling sand, player AABB collision, side-scroll digger mini-project.
- `intermediate-demos.js` — 5 IIFE demos: voxPicking, voxMinePlace, voxFalling,
  voxPlayer, voxDigger. Self-contained helper copy (re-declares the core voxel
  helpers — each tier's demos.js stands alone, per project convention).
- `bundles-intermediate.js` — export bundles for the two button-only demos
  (vox_falling, vox_player); the other three omit `data-demo-id`.

Cross-tier links unlocked everywhere: terraria/index.html card (locked→active),
beginner.html CTA, voxel-worlds/index.html sub-track card, root index.html TOC.

## Key bug found & fixed during verification
**Player jittered ~1px and `onGround` flickered every frame.** Root cause: the
`overlappingTiles` AABB→tile-range helper used `floor((pos + size - 1) / TILE)`
for the far edge. When the player rests *flush* on a tile boundary that formula
reports the row *above* the floor, so gravity sinks the player a sub-pixel
amount undetected, then the collision fires and snaps back — every frame.
Fix: the integer cells a half-open span `[pos, pos+size)` touches are
`floor(pos) .. ceil(pos+size) - 1`. Switching the far edge to `ceil()-1`
detects a sub-pixel overlap the same frame → snap is flush, no jitter, stable
`onGround`. Fixed in all three places (demos.js, the HTML JS+TS code panes,
bundles DEMO_CODE). Verified: y rock-stable at 278 over 10 frames, jitter 0.000.

## Key decisions / learnings
- **Per-axis collision resolution** (move X → resolve → move Y → resolve) is
  what avoids corner-snag; **bottom-up scan** is what stops falling-sand from
  teleporting a whole column in one frame. Both written deliberately as the
  teachable "correct" version.
- **`ceil(end)-1`, not `floor(end-1)`** is the right AABB→cell-range edge — the
  `-1` form silently under-reports on fractional positions. Classic platformer
  off-by-one; cost a real jitter bug here.
- Export scaffold renders only `<button>` controls → demos with sliders/radios
  omit `data-demo-id` (same call as the Beginner tier).
- Bundle TS variants alias `DEMO_CODE_TS = DEMO_CODE` (plain JS is valid TS;
  Sucrase strips nothing) — shaders simulations-tier precedent. The page's
  teaching code blocks still ship hand-written typed TS panes.
- No ARCHITECTURE.md change — the Intermediate tier reuses the existing
  tier-file + bundles pattern exactly; nothing structural is new.

## Verification
- All 5 canvases render real pixels; console clean through every interaction.
- Player physics: walk (x 48→147), full jump (57px / ~3.5 tiles in open air),
  head-bonk correctly caps a jump under a platform, lands back flush, onGround
  stable, zero jitter.
- Falling sand: Run advanced 192 ticks, bottom-third sand pixels 0 → 3456.
- Mining: left-click cleared tile (16,11) stone→air.
- Digger: walk scrolls the follow-camera (player tile 12→20), hotbar works.
- Both export bundles generate valid standalone HTML (8.7 KB / 9.6 KB).
- README.md updated; PROMPT.md appended.

---

# 2026-05-21 (pt.3) — Voxel Worlds: Terraria Expert tier

## Request
"Work on the next iteration" → the Terraria Expert tier (World Generation &
Liquids).

## Delivered (browser-verified, console-clean)
Three new files in `voxel-worlds/terraria/`:
- `expert.html` — 6 sections + recap: noise primitive, surface/biome worldgen,
  caves & ore, cellular-automaton water, tile lighting flood fill, chunked
  infinite worlds.
- `expert-demos.js` — 6 IIFE demos: voxNoise, voxWorldgen, voxCaves, voxWater,
  voxLight, voxChunks. New helpers: smoothNoise2D + fbm2D, surfaceHeight,
  biomeAt, generateTile (pure function of position), generateWorld, stepWater,
  computeLight (BFS flood fill), ChunkedWorld class.
- `bundles-expert.js` — export bundles for the two button-only demos
  (vox_water, vox_light); the four slider/checkbox demos omit `data-demo-id`.

Cross-tier links unlocked: terraria/index.html Expert card (locked→active),
intermediate.html CTA, voxel-worlds/index.html sub-track card, root index.html
TOC.

## Key algorithms / decisions
- **Value noise + fBm**: hash 4 lattice corners, smoothstep-blend, sum octaves
  (double freq, half amplitude). `generateTile(wx,wy,seed)` is a *pure function
  of position* — that purity is exactly what lets chunks generate independently
  and still seam up.
- **Caves = noise iso-band**: carve where `|fbm − 0.5| < width`. An iso-line is
  long and connected → wormy tunnels, not disconnected blobs. A single
  threshold (`fbm > t`) would give blobs instead.
- **Cellular water** = falling-sand + a sideways-spread step. The sideways step
  is what makes it pool instead of pile. Honest limitation noted in the tier
  (binary cells can't equalise levels — a lone droplet wanders).
- **Light flood fill**: BFS/Dijkstra relaxation from sky-exposed + torch tiles,
  subtracting per-tile attenuation (air 0.10, solid 0.24). Flat array used as a
  queue with a `head` pointer to avoid `Array.shift()`'s O(n) cost.
- No ARCHITECTURE.md change — Expert reuses the existing tier-file pattern.

## Bug / polish found during verification
- **seedTorches placed only 1 torch.** It probed 3 fixed columns (14/30/46);
  for seed 21 only one had a cave at a clean air-above-solid spot. Fixed:
  probe ~8 columns (`x += 6`), place into the first 4 that have caves —
  robust to whatever the seed carves. Now 4 torches; verified lighting
  contrast (cave luminance 24 vs lit 563). Fixed in demos.js + bundle.

## Verification
- All 6 canvases render; console clean through every interaction.
- Noise octave slider, worldgen regenerate, cave-width slider all work.
- Water: poured 28 tiles, conserved through a run, 2800 water-blue pixels
  pooled in the bottom rows.
- Lighting: 4 torches seeded, strong dark/lit contrast, glow gradients.
- Chunks: panning grew the generated-chunk count 8 → 14.
- Both export bundles generate valid standalone HTML (vox_water 8.8 KB,
  vox_light 11.1 KB — the latter's hash2D dependency resolved correctly).
- README.md updated; PROMPT.md appended.

---

# 2026-05-21 (pt.4) — Voxel Worlds: Terraria Advanced tier

## Request
"Work on the next iteration" → the Terraria Advanced tier (Persistence, AI &
Lighting Polish).

## Delivered (browser-verified, console-clean)
Three new files in `voxel-worlds/terraria/`:
- `advanced.html` — 5 sections + recap: RLE save/load, A* on a destructible
  grid, day/night & weather, offscreen-canvas tile caching, living-world
  capstone.
- `advanced-demos.js` — 5 IIFE demos: voxPersist, voxPathfind, voxDayNight,
  voxPerf, voxCapstone. New helpers: rleEncode/rleDecode, aStar (returns
  {path, visited}), computeLight with a skyIntensity parameter, an
  offscreen-canvas cache, and a roaming A* enemy in the capstone.
- `bundles-advanced.js` — export bundles for the two button-only demos
  (vox_persist, vox_pathfind); voxDayNight/voxPerf/voxCapstone omit
  `data-demo-id`. A shared `VOX_GEN_SRC` string interpolates the noise +
  worldgen source into both DEMO_CODE bodies.

Cross-tier links unlocked: terraria/index.html Advanced card (locked→active),
expert.html CTA, voxel-worlds/index.html sub-track card, root index.html TOC.
The Terraria sub-track is now 4 of 5 tiers shipped.

## Key algorithms / decisions
- **RLE compression**: store `[value, count]` run pairs, count capped at 255.
  A generated world compressed 6.1× in testing; holes break runs so a
  hand-mined world compresses less (a nice teaching observation).
- **A* on the tile grid**: g + h with a Manhattan heuristic; the grid *is* the
  nav graph, so "re-plan on edit" is free. Open set is a linear-scanned array
  (simple at demo scale; the tier notes a binary heap is the real fix).
- **Day/night**: just the Expert flood-fill with the skylight *seed value*
  turned into `0.06 + 0.94·dayFactor`, dayFactor a cosine of time. Rain is a
  0.7× multiplier on top.
- **Offscreen-canvas caching**: render the static world once to an off-DOM
  canvas, then one `drawImage` blit per frame. Measured cached ≈0 ms vs
  uncached 1.38 ms/frame.
- No ARCHITECTURE.md change — Advanced reuses the tier-file pattern.

## Bug found during verification
- **voxPathfind opened on "no path".** Two random air cells in a cave-riddled
  world are often stranded in separate pockets. First fix (regenerate until a
  path exists) handled `reset()`, but the auto-roaming enemy still picked a
  fresh *random* goal each time it arrived — frequently unreachable. Real fix:
  a `newGoal()` helper that only returns a goal A* can reach (path length > 6),
  used by both `reset()` and the arrival handler. Verified 6/6 resets now open
  with a valid path. Fixed in demos.js + bundle.

## Verification
- All 5 canvases render; console clean through every interaction.
- Persist: Save → 1152 raw bytes → 188 RLE bytes (6.1×).
- Pathfind: 6/6 resets produce a valid path; mining re-routes live.
- Day/night: midnight skylight 0.06 / phase night; noon 1.00 / phase day.
- Perf: cached blit ≈0 ms vs uncached redraw 1.38 ms/frame.
- Capstone: player physics + roaming A* enemy + advancing day/night all run.
- Both export bundles generate valid standalone HTML (vox_persist 10.6 KB,
  vox_pathfind 12.3 KB).
- README.md updated; PROMPT.md appended.

---

# 2026-05-21 (pt.5) — Voxel Worlds: Terraria Simulations tier (sub-track complete)

## Request
"Continue with the next iteration" → the Terraria Simulations tier — the 5th
and final tier of the Terraria sub-track.

## Delivered (browser-verified, console-clean)
Three new files in `voxel-worlds/terraria/`:
- `simulations.html` — 5 sections + recap: cave-carving visualizer, biome
  blend playground, light-propagation BFS stepper, liquid-flow stepper, ore
  distribution generator.
- `simulations-demos.js` — 5 IIFE demos: voxSimCaves, voxSimBiome, voxSimLight,
  voxSimFlow, voxSimOre. Deep-dive visualisers that EXPOSE algorithms (raw
  noise field, BFS frontier ring by ring, per-cell water decision arrows,
  ore-by-depth histogram) rather than hiding them.
- `bundles-simulations.js` — export bundles for the two button-only demos
  (vox_simlight, vox_simflow); the three slider demos omit `data-demo-id`.

Cross-tier links unlocked; the Terraria sub-track is now **5 of 5 — complete**.
voxel-worlds/index.html Terraria card status updated to "All 5 Tiers".

## Key bug found & fixed during verification
**Every demo on simulations.html rendered blank, yet the console was clean.**
Root cause: `simulations-demos.js` declared `const lerp` at the *top level*.
`shared/utils.js` already defines a global `function lerp`. A script-level
`const` that collides with an existing global throws "Identifier 'lerp' has
already been declared" at *instantiation* — before any code runs — so every
function and IIFE in the file silently failed to register. Diagnosis path:
`typeof window.hash2D` was `undefined` (the file's globals never registered)
while `clearCanvas` from utils.js was fine, and `new Function(src)` parsed
clean (Function scope has no global `lerp` to collide with — which is why a
parse-check misses this class of bug). Fix: delete the top-level `const lerp`
and use the utils.js global (same signature). Earlier tiers escaped this only
because their `const lerp` lived *inside* an IIFE.

## Key decisions / learnings
- A "simulations" tier inverts the demo goal: gameplay tiers hide the
  algorithm, sim demos expose it (noise heatmap, BFS frontier outline, water
  decision arrows colour-coded by rule precedence, depth histogram).
- **Self-contained tier files must not top-level-declare names that shadow
  shared/utils.js globals** — documented in ARCHITECTURE.md as a convention.
- 4-neighbour BFS produces diamond-shaped frontier rings (Manhattan distance) —
  visible in the light stepper.

## Verification
- All 5 canvases render (100% painted); console clean through every interaction.
- Caves: method toggle (iso-band ↔ worm-walk), noise overlay toggle.
- Biome: blend slider ramps the colours across thresholds.
- Light: 5 Steps → BFS ring 5, frontier 20 tiles (diamond perimeter ≈ 4×ring).
- Ore: richness 0.06 → 0.20 raised ore count 73 → 333.
- Flow: poured water + 10 steps, decision arrows tally down/diagonal/sideways.
- Both export bundles generate valid standalone HTML (8.9 KB / 9.9 KB).
- README.md + ARCHITECTURE.md updated; PROMPT.md appended.

## Status
The Terraria sub-track is COMPLETE — all 5 tiers (Beginner, Intermediate,
Expert, Advanced, Simulations) shipped and verified. The Noita sub-track
remains a preview index — its tier content is the next body of work.

---

# 2026-05-21 (pt.6) — Voxel Worlds: Noita Beginner tier ("Falling Sand from Scratch")

## Request
"Work on the next iteration" → start the Noita sub-track with its Beginner tier.

## Delivered (browser-verified, console-clean)
Three new files in `voxel-worlds/noita/`:
- `beginner.html` — 4 sections + recap: pixels-not-tiles ImageData engine, the
  falling-sand rule, update order & directional bias, bucket-of-sand sandbox.
- `beginner-demos.js` — 4 IIFE demos: noiEngine (36k-pixel ImageData engine),
  noiRule (coarse rule-stepper with decision arrows), noiOrder (top-down vs
  bottom-up scan, the teleport bug live), noiBucket (paint-walls + pour-sand
  sandbox). Helpers: noiMakeRenderer (ImageData pipeline), stepSand (bottom-up,
  alternating sweep), stepSandNaive (the buggy top-down), noiPaintDisc.
- `bundles-beginner.js` — export bundles for noi_rule and noi_bucket; fully
  self-contained DEMO_CODE, empty `data-deps`, no DEPENDENCY_BUNDLES.

Cross-tier links unlocked: noita/index.html Beginner card (locked→active),
"Coming Soon" section → "Start Building", voxel-worlds/index.html Noita card
("Preview" → "Beginner Ready"), root index.html TOC (Noita gets a nested tier
list). Noita demo IDs use the `noi` prefix (Terraria uses `vox`).

## Key decisions / learnings
- **ImageData rendering** — at pixel granularity (36k cells) fillRect-per-cell
  is too slow. Write RGBA bytes into a `Uint8ClampedArray`, `putImageData` to a
  sim-resolution offscreen canvas, then `drawImage` it scaled up with
  `imageSmoothingEnabled = false`. First use of this technique in the project.
- **Update order is the headline lesson.** A top-down in-place scan re-visits a
  just-moved grain and moves it again → it teleports to the floor in one frame.
  Bottom-up scan fixes it; alternating the horizontal sweep cancels sideways
  drift. noiOrder demonstrates the bug live with a toggle.
- **Walls need no code** — the sand rule only moves a grain into AIR, so any
  non-air material is a static obstacle for free.
- Applied the pt.5 lesson: no top-level `const`/`class` shadowing utils.js
  globals — all names are `noi*`/`NOI_*`.
- No ARCHITECTURE.md change — ImageData rendering is a Canvas2D technique, not
  a new structural pattern; the tier-file layout is unchanged.

## Verification
- All 4 canvases render (100% painted); console clean through every interaction.
- noiEngine: poured sand accumulated to ~1,192 cells, falling continuously.
- noiRule: 3 Steps advanced the tick counter to 3; decision arrows shown.
- noiOrder: toggle flips naive↔correct (button label + info text both update).
- noiBucket: poured sand fell and filled the wall bucket into a conical pile
  (angle of repose emerges from the down/down-diagonal rule).
- Both export bundles generate valid standalone HTML (noi_rule 7.5 KB,
  noi_bucket 7.1 KB).
- README.md updated; PROMPT.md appended.

## Status
voxel-worlds: Terraria sub-track complete (5/5 tiers); Noita sub-track has its
Beginner tier shipped (1/3). Noita Intermediate (liquids, gases, fire,
reactions) and Advanced (performance, material library) remain.
