# 2026-05-31 — New track: Platformer (Celeste / Hollow Knight style) — scaffold

## Request

Plan and build a **Platformer track** (Celeste / Hollow Knight style), prompted by
two screenshots from an earlier session proposing a tiered "character feel"
platformer arc. Make it comprehensive and complete, not a literal copy of the
screenshots ("Add and change whatever is required").

**Decisions (confirmed with user via AskUserQuestion):**
- **Cadence — scaffold first, then one tier per commit** (the netcode / racing-sim /
  roguelike recipe). This commit is the **scaffold** pass.
- **Tier split — my best judgment.** Screenshots are inspiration only; their
  non-monotonic order (Expert "Camera" before Advanced "Moving platforms") is
  remapped into the repo's own difficulty order **Beginner → Intermediate →
  Advanced → Expert → Simulations**. Abilities (wall-jump/dash) live in **Advanced**;
  Intermediate stays pure jump-feel; Expert is camera/parallax/juice.
- **Simulations tier = Performance & Optimization** (culling, pooling, broad-phase,
  chunked/streamed levels, render caching) + the grand capstone — *not* the
  speedrun/frame-rules angle the screenshot suggested.

Plan file: `~/.claude/plans/this-is-a-mighty-blanket.md` (approved).

## Why this track

The repo's first **real-time character-control** track. Every other genre track is
about a *world* (dungeon, race track, voxel planet); this one is about a
*character* and how it **feels** — the most-asked-about 2D-gamedev topics (coyote
time, jump buffering, variable jump, wall-jump, dash, camera) that had no home
anywhere in the repo. The netcode memory even named `platformer/` as the obvious
next track. Composes Fundamentals already taught: intermediate physics/friction/
collision, advanced FSMs, beginner easing + lerp (camera), expert tilemaps.

## Shipped this pass (browser-verified, console-clean)

```
platformer/
  index.html                 ← landing page (mirrors roguelike/index.html): 5
                               roadmap cards (ALL locked "Coming soon"), "What
                               You'll Build" banner ("Summit" capstone preview),
                               Fundamentals prerequisites cross-linking
                               ../intermediate.html#physics/#friction/#collision/#lerp,
                               ../advanced.html#state-machines, ../expert.html#tilemap/#spatial,
                               ../beginner.html#easing, and a live Scaffold Self-Check.
  engine/                    ← cross-tier helpers (the roguelike/engine + netcode/net
                               pattern — names on window, pre-checked vs shared/utils.js)
    tilemap.js               ← PFTile enum (EMPTY/SOLID/ONE_WAY/SLOPE_NE/SLOPE_NW),
                               TileMap (flat Uint8Array, OOB reads EMPTY — unlike the
                               roguelike OOB=WALL, since a platformer runs off edges),
                               PF palette, drawTileMap (coloured blocks; viewport
                               culling built in; one-way = top ledge, slopes = triangles).
    physics.js               ← AABB (top-left x,y + getters + intersects) and
                               moveAndCollide(box, vx, vy, map) — per-axis tile
                               collision (move X & resolve, THEN move Y & resolve using
                               the corrected X). Returns {left,right,up,down}. SOLID-only;
                               one-way/slope handling is taught inline in Advanced later.
    input.js                 ← pfInstallKeys(canvas) — held-key input with edge
                               detection (isDown/pressed/released/axisX + endFrame());
                               click-to-focus + preventDefault on movement keys; and
                               pfLoop(update, render, opts) — the fixed-timestep
                               accumulator loop (PF_MAX_DT=0.1 clamp, maxSteps cap to
                               dodge the spiral of death).
```

Root `index.html` — two surgical edits: a **PLATFORM** nav-button in the Tracks
grid (after ROGUE) and a nested TOC `<li>` after roguelike with five tier sublinks
marked "(coming soon)" (all pointing at the landing page for now — no 404s).
README.md + ARCHITECTURE.md updated.

## Architecture notes (so future sessions don't relearn)

- **Real-time, not turn-based.** Unlike the roguelike (one step per keypress), the
  platformer runs a continuous fixed-timestep loop. Hence `pfLoop` (accumulator) +
  held-key input with edge detection, vs the roguelike's `rlInstallCanvasKeys`
  one-action-per-key model.
- **Engine split (the "promote on 2nd/3rd consumer" rule):** truly cross-tier code
  (tilemap, AABB collision, input/loop) lives in `platformer/engine/` from the
  scaffold. *Taught* tier-specific algorithms (swept-slope collision, one-way
  drop-through, the camera, the feel-kit) stay **inline** in their tier's demos
  file until a 2nd consumer triggers promotion — exactly how roguelike grew
  actors.js / dungeon.js / vision.js / rpg.js. Promotion candidates flagged in the
  plan: `engine/camera.js` (Expert + Sim) and `engine/player.js` (Advanced + Expert + Sim).
- **Per-axis collision is THE primitive.** Move X & snap, then move Y & snap using
  the corrected X — keeps "walk into a wall" and "land on a floor" independent so a
  character doesn't catch on the seams between floor tiles.
- **No top-level shadowing of shared/utils.js globals.** Verified utils.js owns:
  Vector2D, Matrix2D, lerp, clamp, map, randomInt, randomFloat, clearCanvas,
  drawGrid, drawVector, lineIntersection, lineIntersectsRect, createDemoController,
  cartToIso, isoToCart, drawIsoTile, pickTileFromMouse. The engine names (PFTile,
  PF, TileMap, drawTileMap, AABB, moveAndCollide, pfInstallKeys, pfLoop, PF_MAX_DT,
  PF_KEYMAP, PF_EPS) collide with none.
- **Export opt-out:** demos will be keyboard/canvas-driven; export-demo.js only
  renders button controls, so tiers omit `data-demo-id` (no bundles-*.js for now).
  `pf_` namespace reserved for future per-tier bundles.

## Verification (per the plan)

`python3 -m http.server 8765` (existing `.claude/launch.json`) → platformer/index.html.

- **Console zero errors.**
- **Self-check all green:** engine globals present; a falling AABB landed on the
  floor in 16 steps **resting exactly on top (error 0.0000 px)**; walking into the
  right wall stopped flush (`hit.right`).
- **Independent console-test of the other two directions:** left wall stops flush
  (`hit.left`, left edge = 1×TS); ceiling stops flush (`hit.up`, top = 1×TS);
  colAt/rowAt pixel↔tile helpers correct.
- **Canvas renders** the scene (blocks + one-way ledge + slope + cyan box on floor;
  19,440 non-bg px). Screenshot captured.
- **Root integration:** PLATFORM nav-button + TOC entries present; landing page and
  all three engine files return **200**.

## What's next (one tier per commit, Beginner first)

1. **Beginner — "Ground & Gravity":** the platformer loop (teach the fixed timestep
   that `pfLoop` implements), AABB-on-tilemap collision (teach what `moveAndCollide`
   does), grounded + simple jump, run accel/max-speed/friction. Mini: "First Steps".
2. Intermediate — Game Feel · 3. Advanced — Abilities & Moving Geometry ·
   4. Expert — Camera, Parallax & Juice · 5. Simulations — Performance, Scale & the
   grand capstone "Summit".

---

# 2026-05-31 (pt.2) — Platformer: Beginner tier ("Ground & Gravity")

## Request
"Okay, work on the next iteration." → "Continue." — second platformer iteration,
one tier per commit. Topics fixed by the landing-page Beginner card.

## Delivered (browser-verified, console-clean)
```
platformer/
  beginner.html       ← 6 sections + recap (intro, gravity, collision, jump, run,
                        mini-project) + in-page TOC; loads ../shared/utils.js +
                        code-tabs.js + the 3 engine files + beginner-demos.js
  beginner-demos.js   ← 5 IIFE demos + pfDrawBox/pfFocusHint helpers + scroll-to-top
```
`platformer/index.html` — Beginner card flipped to `.active + ready`, "Beginner →"
nav button added, roadmap intro updated ("Beginner is ready, four to come").
Root `index.html` — Beginner TOC sublink now points to `beginner.html` (no
"(coming soon)"). README updated; PROMPT appended.

## The five Beginner demos (`pf_` namespace reserved)
1. **gravityDemo** — the loop in miniature: `vy += G·dt` (clamped to terminal),
   `moveAndCollide(box, 0, vy·dt, map)`. No input; auto re-drops. A fading trail
   makes acceleration visible (gaps grow). Gravity slider + trail toggle.
2. **collisionDemo** — gravity OFF so collision is isolated. Fly a box (arrows/WASD)
   into solids; it stops flush and slides along walls. HUD shows the live
   `{left,right,up,down}` hit flags. "show tile grid" toggle.
3. **jumpDemo** — grounded (`onGround = hit.down`) + a jump that only fires when
   grounded (no double-jump — deliberate, revisited Intermediate). Gravity + jump-
   strength sliders, live `v²/2g` jump-height readout. ←/→ move, ↑/Space jump.
4. **runDemo** — the SAME ←/→ input drives two boxes: TOP snaps to ±max (instant),
   BOTTOM ramps via accel and decays via friction. Velocity bars + accel/friction/
   max sliders. The "instant feels robotic, accel+friction feels alive" lesson.
5. **firstStepsDemo** (capstone "First Steps") — full controller: accel/friction +
   grounded jump + gravity + per-axis tilemap collision on a hand-built level with
   a pit and a gold-flag goal. Reach the flag → win banner; fall in the pit →
   respawn (a free lesson in OOB = empty air).

## Conventions locked in (inherit for the next four tiers)
- **px/second units** for velocities; pass `velocity · dt` to `moveAndCollide`
  (stays sub-tile at the fixed 1/60 s step → no tunneling).
- **Input edges consumed in `update`:** each demo calls `input.endFrame()` at the
  END of its update so `pressed('jump')` single-fires regardless of how many fixed
  steps a frame runs (a press on a 0-step frame just defers, never lost).
- **Standard resolve order each step:** horizontal (accel/friction or constant) →
  jump-if-grounded → gravity+clamp → `moveAndCollide` → zero vy on up/down, zero
  vx on left/right → `onGround = hit.down`.
- **Only top-level globals added by the demos file:** `pfDrawBox`, `pfFocusHint`
  (everything else is a named function EXPRESSION or IIFE-scoped). No collision
  with utils.js/engine.
- **Code blocks use the `.code-pane`/`.code-tabs` contract** that `code-tabs.js`
  toggles (NOT a `.code-block` div) — see "Bug caught" below.

## Bug caught during build (worth remembering)
- **Code-tab markup mismatch.** First pass wrote `<div class="code-block"
  data-lang="ts" style="display:none">`; but `shared/code-tabs.js` toggles
  `.active` on `<pre class="code-pane" data-lang="…">` elements, so the JS/TS tabs
  were dead (TS never showed). Fixed to the real contract: `<pre class="code-pane
  active" data-lang="js">` / `<pre class="code-pane" data-lang="ts">`, no wrapper
  div. Verified in-browser: clicking the TS tab activates the TS pane. **For every
  future tier: copy the `.code-pane` markup from an existing tier page, don't
  invent it.**

## Verification (per the plan)
`python3 -m http.server 8765`. beginner.html:
- **Console zero errors.** All 5 canvases + 18 controls present (`missing: []`);
  all 5 canvases render content; `pfLoop` confirmed animating (gravity box moves
  between two snapshots).
- **Interaction:** code-tabs TS toggle works; jumpDemo flips "grounded ✔ → in the
  air — no jump" on Space; runDemo shows instant lane at max (215) vs accel lane
  ramping. (Synthetic keyup is flaky in the headless preview — the netcode track's
  documented gotcha — so exact mid-transit numbers aren't trusted; qualitative
  behaviour confirmed.)
- **Integration:** landing Beginner card active + nav button; root TOC sublink →
  beginner.html (no "coming soon"); beginner.html + beginner-demos.js return 200.

## What's next
- **Intermediate tier — "Game Feel":** coyote time, jump buffering, variable jump
  height (release-to-cut), apex hangtime + asymmetric/fast-fall gravity, corner
  correction, and a player FSM (idle/run/jump/fall/land). Mini-project "Feel Lab"
  — every assist on a toggle, raw vs juiced A/B. The First Steps controller is the
  starting point; the feel timers (coyote/buffer) get layered onto its update().

---

# 2026-05-31 (pt.3) — Platformer: Intermediate tier ("Game Feel")

## Request
"Okay, work on the next iteration." → "Continue." — third platformer iteration,
one tier per commit. Topics fixed by the landing-page Intermediate card.

## Delivered (browser-verified, console-clean)
```
platformer/
  intermediate.html       ← 8 sections + recap (intro, coyote, buffer, variable,
                            gravity/apex, corner, FSM, mini-project) + in-page TOC;
                            loads ../shared/utils.js + code-tabs.js + 3 engine files
                            + intermediate-demos.js
  intermediate-demos.js   ← 6 IIFE demos + PlayerBody class + RAW_CFG/JUICED_CFG +
                            pfDrawBody/pfFocusHint/pfTimerBar helpers
```
`platformer/index.html` — Intermediate card → `.active + ready`, "Intermediate →"
nav added, roadmap intro "Beginner + Intermediate ready". beginner.html got an
"Intermediate →" nav button. Root `index.html` — Intermediate TOC sublink →
intermediate.html (no "coming soon"). README + PROMPT updated.

## The design: ONE controller, feature flags
Every demo drives the SAME `PlayerBody` class with one feel feature toggled, so
each is felt in isolation and the capstone just turns them all on. `PlayerBody`
holds the whole feel kit behind a `cfg` of knobs; `RAW_CFG` reproduces the
Beginner feel exactly, `JUICED_CFG` is the full kit. This is the tier's lesson so
it lives INLINE in intermediate-demos.js — it's the flagged promotion candidate
for `engine/player.js` once Advanced becomes its 2nd consumer (actors.js pattern).

## The six demos (`pf_` namespace reserved)
1. **coyoteDemo** — refill `coyoteLeft` while grounded, drain in air; jump allowed
   if `onGround || coyoteLeft>0`. Player flashes gold + a timer bar during the grace.
2. **bufferDemo** — `bufferLeft` set on press, drained otherwise; fires the instant
   `onGround||coyote` becomes true. Flashes purple while a press is queued.
3. **variableDemo** — release-to-cut: `if (jumping && !held && vy<0) vy *= cutFactor`.
   Two dotted guides mark short-hop `(v·cut)²/2g` and full `v²/2g` apex heights.
4. **gravityDemo** (apex/asym/fast-fall) — `g = vy<0 ? gravRise : gravFall`, ×apexMult
   when `|vy|<apexBand`, ×fastFallMult when falling + ↓ held. Arc trail; sliders.
5. **cornerDemo** — on a rising head-bonk, try ±1..corner px sideways shifts; if one
   clears the ceiling, apply it (else real ceiling → vy=0). Flashes green on a save.
6. **feelLabDemo** (capstone) — all assists on toggles + Raw/Juiced presets, live
   FSM label drawn over the player, squash-and-stretch on every body.

## Conventions reaffirmed / new
- **Self-contained tier file:** intermediate-demos.js RE-declares pfFocusHint (not
  imported from beginner-demos.js) — only one tier's demos file loads per page.
- **Feature = a zeroable knob:** coyoteMs/bufferMs/apexThreshold = 0, variable/
  corner = false ⇒ feature simply absent (= Beginner behaviour). One code path
  teaches the lesson AND is the capstone.
- **Squash-and-stretch** preserves volume cheaply: w = box.w/squash, h = box.h*squash
  (squashed = wider+shorter on land, stretched = taller on takeoff), relaxed toward
  1 via `squash += (1-squash)*min(1, dt*14)`.
- Code-pane `.code-pane`/`.code-tabs` markup copied from beginner.html (the pt.2
  gotcha) — verified 6/6 JS+TS panes, div balance 52/52.

## Verification (per the plan) — console-tested each mechanic HEADLESSLY
Built a fake input object + stepped PlayerBody at fixed dt (more reliable than
synthetic DOM keys — the netcode flaky-keyup gotcha):
- **variable:** hold→full 99.9px, tap→39.6px (<70% of full), OFF tap→99.9px (=full). ✓
- **coyote:** jump 33ms after leaving ground → vy −532 with 120ms window, +85 (no jump)
  with 0ms. ✓
- **FSM:** idle→run→jump→fall transitions all correct. ✓
- **corner:** ceiling within reach (gap 96px), clip col-7 corner: ON nudged +6px and
  rose to top=214 (74px above the 288 ceiling-bottom); OFF stalled at exactly 288,
  nudge 0. ✓ (First test FAILED — ceiling was 162px overhead, unreachable by a 113px
  jump = a TEST-GEOMETRY bug, not a code bug; same class as the scaffold's 999px wall
  probe. Re-ran with a reachable ceiling + a comparative minTop metric. **Lesson: a
  physics test scenario must be physically reachable or a correct impl still "fails".**)
- Console **zero errors**; all 6 canvases render; 6/6 code-tab containers JS-active;
  PlayerBody/pfLoop/moveAndCollide present; 30/30 controls present (missing []).
- Integration: landing card active + nav; root TOC → intermediate.html (no "coming
  soon"); intermediate.html + intermediate-demos.js return 200. Feel Lab screenshot
  captured (IDLE state label, course, Raw/Juiced + 5 toggles, HUD).

## What's next
- **Advanced tier — "Abilities & Moving Geometry":** wall detection → wall-slide +
  wall-jump (re-grab lockout); dash (cooldown / ground-refresh / dash-cancel);
  one-way platforms (land on top, drop-through on down+jump); slopes (swept collision
  vs SLOPE_NE/NW triangle tiles — the PFTile enum already reserves them); moving
  platforms + conveyors (relative-motion carrying). Mini: a vertical gauntlet.
  PlayerBody is the foundation — and its 2nd consumer, so this is the tier that
  promotes it to engine/player.js.

---

# 2026-05-31 (pt.4) — Platformer: Advanced tier ("Abilities & Moving Geometry")

## Request
"Okay, work on the next iteration." → "Continue." — fourth platformer iteration,
one tier per commit. Topics fixed by the landing-page Advanced card.

## Delivered (browser-verified, console-clean)
```
platformer/
  engine/player.js        ← NEW: PlayerBody + RAW_CFG/JUICED_CFG + PF_STATE_COLOR +
                            pfDrawBody, PROMOTED from intermediate-demos.js (a *move*)
                            and extended with wall-slide/wall-jump + dash + a
                            swappable resolve() hook.
  advanced.html           ← 7 sections + recap (intro, wall, dash, one-way, slope,
                            moving, mini-project) + in-page TOC; loads the 4 engine
                            files (incl. player.js) + advanced-demos.js
  advanced-demos.js       ← 6 IIFE demos + pfResolveWorld + MovingPlatform/
                            pfMovePlatform/pfRidePlatforms + pfDropInput + pfFocusHint/pfBar
  intermediate.html       ← now also loads engine/player.js (before its demos file)
  intermediate-demos.js   ← PlayerBody/configs/pfDrawBody REMOVED (moved to engine);
                            keeps only pfFocusHint/pfTimerBar + the 6 demos
```
`platformer/index.html` — Advanced card → `.active + ready`, "Advanced →" nav;
roadmap intro "Beginner+Intermediate+Advanced ready". intermediate.html got an
"Advanced →" nav. Root `index.html` — Advanced TOC sublink → advanced.html (no
"coming soon"). README + ARCHITECTURE (player.js promotion + resolve() seam) +
PROMPT updated.

## The promotion (the headline architectural move)
PlayerBody graduated to engine/player.js the moment Advanced became its 2nd
consumer — the actors.js/vision.js rule. It's a **MOVE**, not a lib copy:
intermediate.html now loads the engine copy and intermediate-demos.js no longer
declares the class (two `class PlayerBody` on one page = redeclaration error, the
same trap rpg.js documents). Abilities (wall-slide/wall-jump, dash) went INTO
PlayerBody behind zeroable knobs (wallSlideSpeed/dashSpeed = 0 ⇒ off), so the
Intermediate Feel Lab — which sets those to 0 — is byte-unchanged in behaviour.

## The resolve() seam (the extension pattern)
PlayerBody.resolve(box,dx,dy) defaults to plain moveAndCollide; the Advanced demos
assign `body.resolve = (b,dx,dy) => pfResolveWorld(b,dx,dy,map,{...})`. So the
controller learns one-way platforms + slopes with ZERO edits. Corner-correction
still probes SOLID directly (a one-way/slope can't bonk a head).

## The six demos (`pf_` namespace reserved)
1. **wallDemo** — wall-slide (cap fall speed when pressing into a SOLID wall while
   descending) + wall-jump (up + away, then a ~160ms input lock so the impulse
   carries off the wall). Climb a shaft.
2. **dashDemo** — 8-way dash: fixed-duration gravity-free burst, cooldown bar,
   one-per-ground/wall refresh (canDash), fading ghost trail.
3. **oneWayDemo** — pfResolveWorld one-way rule: solid only when descending AND
   prevBottom was at/above the top edge; ↓+jump → drop-through window. pfDropInput
   wrapper turns ↓+jump into a drop request so PlayerBody stays one-way-agnostic.
4. **slopeDemo** — SLOPE_NE/NW sampled by localX under the box centre; "stick"
   term glues feet downhill within ~0.6 tile so walking down doesn't hop.
5. **movingDemo** — MovingPlatform (lerp path) + conveyor (surface velocity);
   pfRidePlatforms carries the rider by the platform's per-step delta BEFORE
   resolving, so you don't slide off. Horizontal mover + vertical lift + belt.
6. **gauntletDemo** (capstone) — one vertical climb: one-way ladder → wall-jump
   shaft → dash gap → moving lift → slope to the goal flag. TS=28, 640×448.

## Verification (per the plan) — headless model-stepping
- **Intermediate page still healthy post-move:** console clean, PlayerBody/
  JUICED_CFG/pfDrawBody global, abilities present (onWall), Feel Lab renders 43,247
  non-bg px (== pt.3 baseline → the move regressed nothing).
- **Advanced algorithms (all PASS):** one-way (lands flush on top / rises through
  unblocked / drops through on demand); slope SLOPE_NE rises to the right (hi
  bottom 259 vs lo 285, both grounded+slope-flagged); moving platform (rider tracks
  the platform delta exactly); wall-jump (detects +1 wall, fires, vy<-100 & vx<0).
- Console **zero errors** on both pages; all 6 Advanced canvases + 17 controls
  present; 5/5 code-tab panes (capstone has no <details>, like other tiers); div
  balance 44/44; all 3 JS files parse. Slope + gauntlet screenshots captured.

## Gotcha re-confirmed (preview rAF throttling)
The preview tab is `visibilityState: hidden`, so requestAnimationFrame is throttled
to 0 ticks — pixel-sampling a demo canvas right after navigation reads 0 non-bg px
even when the code is fine. A preview_screenshot momentarily foregrounds the page
and forces a paint (Feel Lab then read 43k px). So: SCREENSHOT to prove rendering,
HEADLESS MODEL-STEPPING (rAF-independent) to prove behaviour. (Two of my first-pass
console-tests "failed" purely from unreachable test geometry — a slope test with no
floor under the ramp, and a one-way "rises 8px clears the edge" assertion — the
same class as the scaffold's 999px wall probe. Re-ran with reachable scenarios →
all green. Lesson restated: a physics test scenario must be physically reachable.)

## What's next
- **Expert tier — "Camera, Parallax & Juice":** follow camera (lerp + deadzone +
  look-ahead), screen shake, parallax scrolling, an animation state machine driving
  the body's shapes, particles (run dust / landing puffs), squash-and-stretch +
  hitstop. Mini: a scrolling, juicy level. The camera is the flagged promotion
  candidate for engine/camera.js once the Simulations capstone reuses it. This is
  the first tier where the level is bigger than the screen (drawTileMap already
  takes an originX/originY camera offset + culls, so it's ready).

---

# 2026-05-31 (pt.5) — Platformer: Expert tier ("Camera, Parallax & Juice")

## Request
"Okay, work on the next iteration." — fifth platformer iteration, one tier per
commit. Topics fixed by the landing-page Expert card.

## Delivered (browser-verified, console-clean)
```
platformer/
  expert.html        ← 7 sections + recap (intro, camera, shake, parallax, anim,
                       particles, mini-project) + in-page TOC; loads the 4 engine
                       files (incl. player.js) + expert-demos.js
  expert-demos.js    ← 6 IIFE demos + Camera + ParticleField + pfDrawCharacter +
                       drawParallax + pfWideLevel + pfFocusHint/pfBar
```
`platformer/index.html` — Expert card → `.active + ready`, "Expert →" nav; roadmap
intro "Beginner+Intermediate+Advanced+Expert ready; Simulations next". advanced.html
got an "Expert →" nav. Root `index.html` — Expert TOC sublink → expert.html (no
"coming soon"). README + ARCHITECTURE (Camera inline, flagged for engine/camera.js)
+ PROMPT updated.

## The camera-space convention (shared by every scrolling demo)
clearCanvas → drawParallax (SCREEN space) → ctx.translate(-cam.originX,-cam.originY)
→ drawTileMap({cullToCanvas:false}) + entities (WORLD coords) → restore → HUD/overlays
(screen space). One translate; everything in the world Just Works. (drawTileMap's
originX/originY culling is bypassed here with cullToCanvas:false since the translate
already positions the world — culling is the Simulations tier's lesson.)

## The six demos (`pf_` namespace reserved)
1. **cameraDemo** — Camera.follow with smoothing slider (0 = rigid), deadzone box
   (drawn dashed) + look-ahead toggles, and a world MINIMAP inset (camera window
   rect + player dot). Wide 48-col level.
2. **shakeDemo** — trauma model: addTrauma() on buttons / hard landing; shake =
   trauma² (small trauma stays calm), high-freq sines, linear decay. Trauma bar.
3. **parallaxDemo** — drawParallax: 3 hill layers at factors 0.15/0.38/0.66; OFF =
   all factor 1 (flat). The offset = -(camX * factor) is the whole trick.
4. **animDemo** — pfDrawCharacter: the controller's FSM state (idle/run/jump/fall/
   land/wall/dash) picks a pose; `phase` advances limbs (faster when running).
   Procedural head+torso+legs+arms, asset-free.
5. **particlesDemo** — ParticleField (capped array, vel+gravity+life-fade): run
   dust behind the feet, radial landing puff scaled by impact speed.
6. **juiceLabDemo** (capstone) — a 52-col scrolling level composing camera +
   parallax + shake + particles + hitstop, each on a toggle (+ All on/off), live
   FSM character, goal flag. Hitstop = freeze the whole sim ~3 frames on a hard
   landing (shake still plays) → reads as impact weight.

## Camera design notes (for the engine/camera.js promotion later)
- follow(tx,ty,dt,{smoothMs,deadzoneW,deadzoneH,lookAhead,lookDir}). Deadzone: only
  move when the target leaves a central box; else keep centred. Smoothing:
  a = 1 - exp((ln 0.01 / (smoothMs/1000)) * dt) (99% of the gap in smoothMs;
  smoothMs 0 ⇒ a=1 ⇒ rigid). Then clamp x∈[0, worldW-viewW], y likewise.
- Shake: addTrauma(t) clamps trauma to [0,1]; updateShake uses trauma² × sum-of-
  sines; originX/originY = round(x+shakeX). Renderer translates by origin*.

## Verification (per the plan) — headless Camera tests + screenshots
- **Camera (all PASS):** follow centres a far target (cam.x → target-viewW/2 = 680);
  clamps to BOTH world edges (right→160, left→0); deadzone HOLDS while the target
  is inside the box and RELEASES when it exits; shake = trauma² (0.5→0.25, clamps
  at 1) and trauma settles to 0 (shakeX→0) after decay.
- Console **zero errors**; all 6 canvases + 26 controls present (missing []);
  globals present; 5/5 code panes; div balance 46/46; expert-demos.js parses.
- Screenshots: camera demo (dashed deadzone box + minimap window+dot) and Juice Lab
  (parallax depth hills + procedural character + scrolled camera) both render.
- Integration: landing Expert card active + nav; root TOC → expert.html (no "coming
  soon"); expert.html + expert-demos.js return 200.

## What's next — the FINAL tier
- **Simulations — "Performance, Scale & The Whole Game":** viewport culling
  (drawTileMap already supports it via cullToCanvas + originX/originY — this is
  where it becomes the lesson), object pooling (rebuild ParticleField as a true
  pool, zero per-frame alloc), broad-phase collision (spatial grid for many
  entities), chunked/streamed big levels + offscreen render caching of the static
  tile layer (the voxel-track trick). Promote Camera → engine/camera.js (2nd
  consumer). Grand capstone **"Summit"** — the complete playable platformer
  composing every system (tilemap collision + full feel kit + abilities + slopes +
  moving geometry + camera + parallax + juice + the perf techniques) with a goal
  and a timer. Fixed-timestep accumulator (from Beginner) is its stable backbone.

---

# 2026-05-31 (pt.6) — Platformer: Simulations tier — TRACK COMPLETE

## Request
"Okay, work on the next iteration." — sixth and final platformer iteration. Topics
fixed by the landing-page Simulations card.

## Delivered (browser-verified, console-clean)
```
platformer/
  engine/camera.js        ← NEW: Camera PROMOTED from expert-demos.js (a *move*;
                            2nd consumer = this capstone). engine/ now = tilemap,
                            physics, input, player, camera (5 modules).
  simulations.html        ← 6 sections + completion banner (intro, culling, pooling,
                            broadphase, chunk, capstone) + in-page TOC; loads all 5
                            engine files + simulations-demos.js
  simulations-demos.js    ← 5 demos + 3 NEW perf systems (ParticlePool, SpatialGrid,
                            ChunkCache) + RE-DECLARED helpers (pfResolveWorld,
                            MovingPlatform/pfRidePlatforms, drawParallax,
                            pfDrawCharacter, pfDropInput) for the capstone + pfPerfMeter
  expert.html             ← now also loads engine/camera.js
  expert-demos.js         ← `class Camera` REMOVED (moved to engine/camera.js)
```
`platformer/index.html` — Simulations card → `.active + ready`, "Simulations →"
nav; roadmap intro "All five tiers complete". expert.html got a "Simulations →"
nav. Root `index.html` — Simulations TOC sublink → simulations.html; **last
"(coming soon)" marker gone — every platformer sublink now live.** README +
ARCHITECTURE (camera promotion + track complete) + PROMPT updated.

## The camera promotion (the last "≥2 consumer ⇒ engine/" move)
Camera was taught inline in Expert; the Summit capstone is its 2nd consumer, so it
MOVED to engine/camera.js (expert.html loads it, expert-demos.js dropped the
class — verified the Expert page still console-clean + constructs Camera from the
global). Mirrors the PlayerBody move (Intermediate→engine when Advanced consumed it).

## The five demos (`pf_` namespace reserved)
1. **cullingDemo** — 200-col map auto-pans; toggle drawTileMap cullToCanvas; HUD
   shows tiles-considered (≈view vs whole map) + ms/frame. Culling = O(view).
2. **poolingDemo** — fountain; POOLED (ParticlePool, fixed array, swap-remove,
   allocs flat) vs NAIVE (new obj/spawn + filter, allocs climb). Counters contrast.
3. **broadphaseDemo** — N bouncing balls; SpatialGrid (cell buckets, check own+8
   neighbours, id-ordered dedup) vs naive n(n-1)/2; live pair-check counts; slider.
4. **chunkDemo** — static map; ChunkCache renders each 8×8 chunk to an offscreen
   canvas once then blits; click a tile → markDirtyTile → only that chunk
   re-renders (counter ticks 1). NAIVE redraws all tiles every frame.
5. **summitDemo** (GRAND CAPSTONE "Summit") — wide 80-col level: one-way ladder,
   slope hill, dash pit, wall-jump shaft, moving platform, goal flag. Composes
   PlayerBody (full feel + wall + dash) + engine Camera (follow/deadzone/lookahead/
   shake) + parallax + ParticlePool (dust + landing puffs) + hitstop + FSM
   character + viewport-culled drawTileMap + a count-up timer & best time.

## Verification (per the plan) — headless perf-system tests + screenshots
- **Expert page survives the Camera move:** console clean; Camera global from
  engine; ParticleField/drawParallax/pfDrawCharacter intact.
- **Sim perf systems (all PASS):** ParticlePool — 50 alive after spawn, allocs flat
  at 100 (preallocated), 0 alive after lifetime (no new allocation); SpatialGrid —
  386 pair checks vs naive 7,140 at 120 balls (~18×); ChunkCache — 15 chunks render
  once, then 0 (cached), then exactly 1 after a tile edit (only the dirty chunk).
- Console **zero errors** on both pages; all 5 Sim canvases + 13 controls present;
  globals present (3 new perf classes + re-declared helpers + engine Camera); 4/4
  code panes (capstone has no <details>); div balance 37/37; all JS parses.
- Summit screenshot: timer HUD, parallax depth, one-way platforms, FSM character,
  culled scrolling — a complete game. Integration: landing Sim card active + nav;
  root TOC → simulations.html; **0 "(coming soon)" markers remain**; all files 200.

## TRACK TOTAL — COMPLETE
**5 tiers, 28 demos, 5 engine modules** (tilemap, physics, input, player, camera).
Beginner (loop + AABB collision) → Intermediate (game feel) → Advanced (abilities +
moving geometry) → Expert (camera + juice) → Simulations (perf + the whole game,
"Summit"). The fixed-timestep determinism from the scaffold threads through to the
capstone. Two engine promotions happened on schedule (PlayerBody at Advanced,
Camera at Simulations) — the "≥2 consumer ⇒ engine/" rule, exactly as planned.
**PLATFORMER TRACK COMPLETE.**

## Cross-track conventions reaffirmed this track (for future work)
- Scaffold-first, one tier per commit; each tier ends in a playable mini-project
  that seeds the next; verify console-clean + docs finished before the commit msg.
- "Feature = a zeroable knob" lets one code path be both the lesson (toggled on in
  isolation) and the capstone (all on). Used for the whole feel kit + abilities.
- Promote a taught helper to engine/ ONLY on its 2nd consumer (PlayerBody, Camera);
  the capstone tier may re-declare compact helpers inline (self-contained) rather
  than load sibling tier-demos files.
- Verify physics HEADLESSLY (construct the model, step fixed dt) — far more reliable
  than synthetic DOM input, and rAF is throttled while the preview tab is hidden so
  screenshot-to-paint + headless-step is the trustworthy combo. ALWAYS make the test
  scenario physically reachable (the 999px-wall / unreachable-ceiling / no-floor-
  under-slope mistakes all "failed" a correct implementation).
- Copy the `.code-pane`/`.code-tabs` markup from an existing tier page — never
  reinvent it (the dead-TS-tab bug from the Beginner tier).
